# Global keys

<!-- Generated by (write-manual! DIR) in scheme/packages/howto.scm. Do not edit: change the source and run bin/docs. -->

Every key in the global map, except the keys that insert their own character. A mode map can shadow a key in its own buffers. `C-h b` shows the keys in force in one buffer.

| Key | Command | What it does |
|---|---|---|
| `<delete>` | `delete-char` | Delete the character after point |
| `<down>` | `next-line` | Move point down one line |
| `<end>` | `end-of-line` | Move point to the end of the line |
| `<home>` | `beginning-of-line` | Move point to the beginning of the line |
| `<left>` | `backward-char` | Move point one character backward |
| `<next>` | `scroll-up-command` | Scroll text upward nearly a full screen |
| `<prior>` | `scroll-down-command` | Scroll text downward nearly a full screen |
| `<right>` | `forward-char` | Move point one character forward |
| `<up>` | `previous-line` | Move point up one line |
| `C-+` | `text-scale-increase` | Make this buffer's text larger |
| `C-.` | `embark-act` | Act on the thing at point |
| `C-/` | `undo` | Undo the last change |
| `C-<left>` | `backward-word` | Move point backward one word |
| `C-<right>` | `forward-word` | Move point forward one word |
| `` C-M-` `` | `popper-toggle-type` | Make this popup an ordinary buffer, or make this buffer a popup |
| `C-M-b` | `backward-sexp` | Move backward across one balanced expression |
| `C-M-d` | `down-list` | Move forward down one level of parentheses |
| `C-M-f` | `forward-sexp` | Move forward across one balanced expression |
| `C-M-i` | `completion-at-point` | Perform completion on the text around point |
| `C-M-u` | `backward-up-list` | Move backward out of one level of parentheses |
| `C-M-v` | `scroll-other-window` | Scroll the next window up nearly a full screen |
| `C-SPC` | `set-mark-command` | Set the mark where point is; with a prefix, go back to the previous mark |
| `C-_` | `text-scale-decrease` | Make this buffer's text smaller |
| `` C-` `` | `group-next-mode-buffer` | Walk this buffer's mode in this group, most recently used first |
| `C-a` | `beginning-of-line` | Move point to the beginning of the line |
| `C-b` | `backward-char` | Move point one character backward |
| `C-c ! a` | `annotate-add` | Annotate the region or this line |
| `C-c <left>` | `winner-previous` | Switch to the previous window and buffer arrangement |
| `C-c <right>` | `winner-next` | Switch to the next window and buffer arrangement |
| `C-c A` | `morg-agenda` | Show the agenda: dated entries from your morg files |
| `C-c C-a` | `app-preview` | Run the current buffer as an HTML app |
| `C-c C-r` | `app-reload` | Reload every running app |
| `C-c C-v` | `preview-mode` | Toggle rendered preview of the current buffer |
| `C-c RET` | `chat-companion-ask` | Ask the group chat without leaving this buffer |
| `C-c a a` | `agent-goto-attention` | Jump to the first thread needing attention |
| `C-c a d` | `doppler` | List and manage Doppler secret names |
| `C-c a m` | `mcp-hub` | List every MCP server: status, tools, presets |
| `C-c a n` | `agent-open` | Prompt for a task and spawn a new agent thread |
| `C-c a s` | `sockets` | List every socket the daemon holds. To open a client connection to another program, use endpoint-register! |
| `C-c b` | `llm-configure` | LLM setup |
| `C-c c` | `chat` | Open the group chat for this buffer |
| `C-c d` | `group-describe` | Ask the LLM to write this group's description |
| `C-c e` | `llm-rewrite` | Rewrite the region with the LLM into a diff block below it; with one waiting, decide it or ask again |
| `C-c g` | `group-add` | Put the selected buffers, else this buffer, in a group |
| `C-c l` | `copy-buffer-link` | Copy an compos:// link to this buffer and line to the clipboard |
| `C-c m` | `llm-set-model` | Choose the model for M-o in this buffer |
| `C-c n` | `chat-new` | Start a new chat buffer; with a prefix, choose or create its group |
| `C-c q` | `llm-ask` | Ask the LLM from anywhere via the minibuffer |
| `C-c r` | `chat-send-region` | Add the region to the chat buffer as context |
| `C-c s` | `scratch-buffer` | Toggle between this buffer and its plain scratch buffer |
| `C-c w` | `chat-companion` | Toggle between a work buffer and its group chat |
| `C-d` | `delete-char` | Delete the character after point |
| `C-e` | `end-of-line` | Move point to the end of the line |
| `C-f` | `forward-char` | Move point one character forward |
| `C-g` | `keyboard-quit` | Quit the current operation: cancel the prompt, close the dashboard, or clear the mark |
| `C-h ?` | `help-for-help` | List every help command with its key, and every task recipe |
| `C-h B` | `keys` | Every key in force here, and the edits |
| `C-h C-h` | `help-for-help` | List every help command with its key, and every task recipe |
| `C-h a` | `apropos` | Search the whole editor by words or intent; literal hits rank before cached semantic hits |
| `C-h b` | `describe-bindings` | Show every key binding, this buffer's first |
| `C-h e` | `view-messages` | Display the structured \*Messages\* buffer |
| `C-h h` | `how-do-i` | Pick a task and read how to do it; an empty answer lists every task |
| `C-h k` | `describe-key` | Show what one key does: press the key, read its page |
| `C-h m` | `describe-mode` | Show this buffer's mode, its keys and what they do |
| `C-h t` | `help-with-tutorial` | Select the Compos learn-by-doing tutorial, resuming saved progress |
| `C-k` | `kill-line` | Kill text from point to end of line |
| `C-l` | `recenter-top-bottom` | Recenter point in the window |
| `C-n` | `next-line` | Move point down one line |
| `C-p` | `previous-line` | Move point up one line |
| `C-r` | `isearch-backward` | Do incremental search backward |
| `C-s` | `isearch-forward` | Do incremental search forward |
| `C-t` | `telemetry-toggle` | Show the telemetry list, or take it away |
| `C-u` | `universal-argument` | Start or multiply the next command's prefix argument |
| `C-v` | `scroll-up-command` | Scroll text upward nearly a full screen |
| `C-w` | `kill-region` | Kill the text between point and mark |
| `C-x 0` | `delete-window` | Delete the selected window |
| `C-x 1` | `delete-other-windows` | Make the selected window the only one |
| `C-x 2` | `split-window-below` | Split the window in two, one above the other |
| `C-x 3` | `split-window-right` | Split the window in two, side by side |
| `C-x 4 f` | `find-file-other-window` | Find a file and display it only in another window |
| `C-x <left>` | `previous-buffer` | Switch to the previously used buffer (again = deeper) |
| `C-x <right>` | `next-buffer` | Walk back toward the most recently used buffer |
| `C-x ?` | `modeline-expand` | Toggle this buffer's expanded modeline panel |
| `C-x C-SPC` | `pop-global-mark` | Go back to the last mark set in any buffer |
| `C-x C-b` | `ibuffer` | List buffers by group in a buffer |
| `C-x C-c` | `chat-list` | Switch to a chat, by its name or by a word somebody said in it |
| `C-x C-d` | `dired` | Prompt for a directory and open it in Dired |
| `C-x C-e` | `eval-last-sexp` | Evaluate the region, else the sexp before point, and echo the value |
| `C-x C-f` | `find-file` | Visit a file, prompting with filename completion |
| `C-x C-g a` | `group-add` | Put the selected buffers, else this buffer, in a group |
| `C-x C-g m` | `group-move` | Move the selected buffers, else this buffer, to one group |
| `C-x C-g n` | `group-new` | Create and enter an empty group, or seed it with the selection |
| `C-x C-g r` | `remove-group-from-buffer` | Toggle this buffer out of the groups it belongs to; C-g applies |
| `C-x C-g v` | `group-new-from-visible` | Found a group from every visible work buffer, with the windows as its first layout |
| `C-x C-q` | `read-only-mode` | Toggle whether this buffer refuses edits |
| `C-x C-s` | `save-buffer` | Save the current buffer to its file |
| `C-x C-t` | `switch-window-buffer` | Choose another buffer from this window's title |
| `C-x C-w` | `write-file` | Write the buffer to a file; the buffer becomes that file's buffer |
| `C-x C-x` | `exchange-point-and-mark` | Exchange positions of point and mark |
| `C-x G` | `switch-groups` | Switch group or project, then pick a buffer or file in it |
| `C-x b` | `ibuffer-prompt` | Switch to a buffer with the plain minibuffer list |
| `C-x c` | `chat-prompt` | Switch to a chat with the plain minibuffer list |
| `C-x d` | `daemons` | List daemons and switch this browser tab |
| `C-x e` | `window-eat` | Eat the neighboring pane and take its space |
| `C-x C-g C-g` | `group-switch-last` | Switch back to the group this frame just left |
| `C-x C-g b` | `group-members` | Open the switcher on this group's members |
| `C-x g` | `group-switch` | Switch to a group and restore its layout |
| `C-x C-g l` | `groups` | The groups board: switch, describe, set noise |
| `C-x C-g n` | `group-new` | Create and enter an empty group, or seed it with the selection |
| `C-x C-g p` | `group-pin` | Toggle a frame pin that keeps the current group through window changes |
| `C-x C-g s` | `tile-all` | Open the current group or project in locked columns |
| `C-x k` | `kill-buffer` | Kill a buffer, defaulting to the current one |
| `C-x l` | `window-layout` | Choose a tiling layout for visible buffers; the choice is the frame's target layout |
| `C-x m` | `load-mode` | Choose a mode by name and put this buffer in it |
| `C-x n N` | `narrow-context-also` | Narrow the view and the LLM context to the same region or Morg section |
| `C-x n n` | `narrow-to-region` | Show only the text between point and mark |
| `C-x n w` | `widen` | Show the complete current buffer |
| `C-x o` | `other-window` | Select another window in cyclic order |
| `C-x p d` | `project-dired` | Open dired at the current project's root |
| `C-x p f` | `project-find-file` | Find a file in the current project (git-aware, ignores ignored) |
| `C-x p g` | `project-ripgrep` | Search the project with ripgrep, preview the matches, jump to one |
| `C-x p k` | `project-kill-all` | Kill every buffer in the current project, asking about unsaved files |
| `C-x p p` | `project-switch-project` | Enter a known project's group, then find a file in it |
| `C-x p s` | `project-scratch` | Toggle between this buffer and its project's scratch buffer |
| `C-x r +` | `append-to-register` | Append the region's text to a register |
| `C-x r C-SPC` | `point-to-register` | Save point and the buffer in a register |
| `C-x r M` | `bookmark-set-no-overwrite` | Set a bookmark unless its name exists |
| `C-x r SPC` | `point-to-register` | Save point and the buffer in a register |
| `C-x r b` | `bookmark-jump` | Jump to a named bookmark |
| `C-x r i` | `insert-register` | Insert a register's text at point |
| `C-x r j` | `jump-to-register` | Go to a saved point, or restore saved windows |
| `C-x r l` | `list-bookmarks` | Show the bookmark management table |
| `C-x r m` | `bookmark-set` | Set or replace a named bookmark at point |
| `C-x r s` | `copy-to-register` | Save the region's text in a register |
| `C-x r v` | `view-register` | Say what a register holds |
| `C-x r w` | `window-configuration-to-register` | Save the frame's windows in a register |
| `C-x r x` | `copy-to-register` | Save the region's text in a register |
| `C-x u` | `undo` | Undo the last change |
| `C-x v l` | `buffer-log` | Show the provenance of this buffer: every accepted revision |
| `C-x w` | `workspaces` | List workspaces and open one in a new browser tab |
| `C-y` | `yank` | Reinsert the last killed text at point |
| `DEL` | `delete-backward-char` | Delete the character before point |
| `ESC` | `keyboard-quit` | Quit the current operation: cancel the prompt, close the dashboard, or clear the mark |
| `M-%` | `query-replace` | Replace literal text with confirmation |
| `M-,` | `lsp-pop-marker` | Return to where the last jump started |
| `M--` | `negative-argument` | Negate the next command's prefix argument |
| `M-.` | `goto-thing-at-point` | Follow the address at point, else go to the definition |
| `M-/` | `completion-at-point` | Perform completion on the text around point |
| `M-0` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-1` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-2` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-3` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-4` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-5` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-6` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-7` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-8` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-9` | `digit-argument` | Add a digit to the next command's prefix argument |
| `M-:` | `eval-expression` | Evaluate a Scheme expression from the minibuffer |
| `M-<` | `beginning-of-buffer` | Move point to the beginning of the buffer |
| `M-<down>` | `scroll-other-window` | Scroll the next window up nearly a full screen |
| `M-<left>` | `backward-word` | Move point backward one word |
| `M-<right>` | `forward-word` | Move point forward one word |
| `M-<up>` | `scroll-other-window-down` | Scroll the next window down nearly a full screen |
| `M->` | `end-of-buffer` | Move point to the end of the buffer |
| `M-?` | `contextual-help` | Describe what is here: the name at point, this buffer, its mode and its keys |
| `M-DEL` | `backward-kill-word` | Kill characters backward to the start of a word |
| `M-S-<left>` | `group-tab-left` | Switch to the group shown immediately to the left in the top bar |
| `M-S-<right>` | `group-tab-right` | Switch to the group shown immediately to the right in the top bar |
| `M-SPC` | `set-mark-command` | Set the mark where point is; with a prefix, go back to the previous mark |
| `` M-` `` | `popper-cycle` | Show the next popup in the popup window |
| `M-b` | `backward-word` | Move point backward one word |
| `M-d` | `kill-word` | Kill characters forward to the end of a word |
| `M-f` | `forward-word` | Move point forward one word |
| `M-g M-g` | `goto-line` | Go to a line number read from the minibuffer |
| `M-g g` | `goto-line` | Go to a line number read from the minibuffer |
| `M-g i` | `imenu` | Jump to a definition in this buffer |
| `M-m` | `back-to-indentation` | Move point to the first non-space on this line |
| `M-o` | `llm-send-buffer` | Send this document to the LLM and stream its reply below the block at point; with C-u, choose where the reply goes |
| `M-s t` | `occur-ts` | Show tree-sitter query captures for this buffer |
| `M-v` | `scroll-down-command` | Scroll text downward nearly a full screen |
| `M-w` | `copy-region-as-kill` | Save the region as if killed, but don't kill it |
| `M-x` | `execute-extended-command` | Run a command by name, with its keybinding and doc alongside |
| `M-y` | `yank-pop` | Replace just-yanked text with an earlier kill |
| `M-\|` | `llm-pipe-region` | Pipe the region through the LLM into \*llm\* |
| `RET` | `preview-newline` | Insert one newline in a rendered page |
| `S-<left>` | `previous-buffer` | Switch to the previously used buffer (again = deeper) |
| `S-<right>` | `next-buffer` | Walk back toward the most recently used buffer |
| `S-RET` | `newline` | Insert a newline at point |
| `TAB` | `indent-for-tab` | Indent by inserting two spaces |
| `s-)` | `text-scale-reset` | Give this buffer the normal text size |
| `s-+` | `text-scale-increase` | Make this buffer's text larger |
| `s--` | `ui-scale-decrease` | Make the whole application's text smaller |
| `s-0` | `ui-scale-reset` | Give the whole application the normal text size |
| `s-<down>` | `group-next-buffer` | Walk every buffer in this group, most recently used first |
| `s-<left>` | `focus-left` | Select the window to the left |
| `s-<right>` | `focus-right` | Select the window to the right |
| `s-<up>` | `group-previous-buffer` | Walk every buffer in this group, the other way |
| `s-=` | `ui-scale-increase` | Make the whole application's text larger |
| `s-S-<down>` | `window-down` | Move this window downward with its complete buffer stack |
| `s-S-<left>` | `window-left` | Move this window leftward with its complete buffer stack |
| `s-S-<right>` | `window-right` | Move this window rightward with its complete buffer stack |
| `s-S-<up>` | `window-up` | Move this window upward with its complete buffer stack |
| `s-_` | `text-scale-decrease` | Make this buffer's text smaller |
| `s-p` | `command-palette` | Find an action by intent across command docs and recipes |
